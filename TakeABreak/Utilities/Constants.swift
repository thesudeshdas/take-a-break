import Foundation

enum AppConstants {
    static let appName = "Take a Break"
    static let defaultWorkMinutes = 20
    static let defaultBreakSeconds = 20
    static let defaultPreBreakWarningSeconds = 10
    static let smartPausePollIntervalSeconds: TimeInterval = 3
}
